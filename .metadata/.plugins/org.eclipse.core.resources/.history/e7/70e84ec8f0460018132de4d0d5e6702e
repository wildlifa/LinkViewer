package gui;

import java.awt.AlphaComposite;
import java.awt.BasicStroke;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.RenderingHints;


import javax.swing.JComponent;

@SuppressWarnings("serial")
public class DrawingPanel extends JComponent {
    
	public DrawingPanel(){
		repaint();
	}
	
	 public void paint(Graphics g)
     {
		 Graphics2D graphicSettings = (Graphics2D)g;

             
		 graphicSettings.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
             
             // Defines the line width of the stroke
             
		 graphicSettings.setStroke(new BasicStroke(2));


             
             // Eliminates transparent setting below
             
		 graphicSettings.setComposite(AlphaComposite.getInstance(AlphaComposite.SRC_OVER, 1.0f));
             
//		 graphicSettings.draw(new Line2D.Double(30, 30, 50, 50));
//		 
//		 graphicSettings.setPaint(Color.CYAN);
//		 graphicSettings.fill(new Rectangle2D.Double(5, 5, 300, 300));


     }
}
